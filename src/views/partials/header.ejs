<!DOCTYPE html>
<html lang="sv">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <title>Vägkrogen<%= title %></title>
    <link rel="stylesheet" href="css/main.css">
    <link rel="icon" type="image/x-icon" href="images/favicon.ico">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
</head>

<body>
    <div id="wrapper">
        <a href="#maincontent" class="skiplink button">Gå till huvudinnehåll</a>
        <header>
            <a href="index">
                <h1>Vägkrogen</h1>
            </a>
            <p class="fancy">En klassisk vägkrog!</p>
            <a href="admin" class="fa fa-heart adminlink"></a>
        </header>
        <nav>
            <ul>
                <li <% if(current == "start") {%> class="current" <% } %>><a href="index">Startsida</a></li>
                <li <% if(current == "about") {%> class="current" <% } %>><a href="about">Om oss</a></li>
                <li <% if(current == "menu") {%> class="current" <% } %>><a href="menu">Meny</a></li>
                <% if(current == "login") {%> <li class="current"><a href="login">Logga in</a></li> <% } %>
                <% if(current == "admin") {%> <li class="current"><a href="admin">Admin</a></li> <% } %>
                <% if(current == "404") {%> <li class="current"><a href="404">Kattbild</a></li> <% } %>
            </ul>
        </nav>
        <main>